server:
  port: 30000

spring:
  application:
    name: service-engine-takeaway-v1
  cloud:
    nacos:
      server-addr: 121.36.195.98:8848
      config:
        namespace: mall
        file-extension: yaml
        extension-configs:
          - data-id: service-engine-takeaway-v1.yml
          - data-id: mysql-service-engine-takeaway-v1.yml
          - data-id: refresh-service-engine-takeaway-v1.yml
            refresh: true
      discovery:
        namespace: ${spring.cloud.nacos.config.namespace}
