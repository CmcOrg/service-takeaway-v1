server:
  port: 30000

spring:
  application:
    name: service-mall-v1
  cloud:
    nacos:
      server-addr: 139.196.121.3:8848
      config:
        namespace: CmcOrg
        file-extension: yaml
        extension-configs:
          - data-id: project-engine-web-v1.yml
          - data-id: mysql-engine-web-v1.yml
          - data-id: refresh-engine-web-v1.yml
            refresh: true
      discovery:
        namespace: ${spring.cloud.nacos.config.namespace}
